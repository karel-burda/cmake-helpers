stages:
  - tests

jobs:
  include:
     - stage: tests
       dist: trusty
       os: linux
       script:
         - set -e

         - cmake -Bbuild -H. -DINTEGRATION_TESTS:BOOL=ON

         - set +e
